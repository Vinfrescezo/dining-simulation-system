# 后端 HTTP 地址。如果同源部署可留空，开发环境默认走 Vite proxy。
VITE_API_BASE_URL=

# 后端 WebSocket 地址。示例：ws://localhost:8080/ws/simulation
VITE_WS_URL=ws://localhost:8080/ws/simulation

# true：无后端也能直接演示；false：只连接后端。
VITE_ENABLE_LOCAL_FALLBACK=true
